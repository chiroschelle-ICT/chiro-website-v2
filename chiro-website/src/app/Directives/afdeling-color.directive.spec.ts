import { AfdelingColorDirective } from './afdeling-color.directive';

describe('AfdelingColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AfdelingColorDirective();
    expect(directive).toBeTruthy();
  });
});
